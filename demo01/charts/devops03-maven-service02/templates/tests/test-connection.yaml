apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops03-maven-service.fullname" . }}-test-connection"
  labels:
    {{- include "devops03-maven-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops03-maven-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
