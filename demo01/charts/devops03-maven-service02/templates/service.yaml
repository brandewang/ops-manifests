apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops03-maven-service.fullname" . }}
  labels:
    {{- include "devops03-maven-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops03-maven-service.selectorLabels" . | nindent 4 }}
